
  # Save CSS File (복사)

  This is a code bundle for Save CSS File (복사). The original project is available at https://www.figma.com/design/rWqZmTKIsGW6hqnO1z5iSO/Save-CSS-File--%EB%B3%B5%EC%82%AC-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  